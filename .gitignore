node_modules/
__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
.env

# Pipeline outputs / inputs (large, generated)
analiza/out/
analiza/input/raw/uploads/
scrapper/media_sample/
scrapper/cmc_out/

# Large raw scrapper datasets (>1MB, derivable from running scrapers)
scrapper/symbols_table.json
scrapper/symbols_table_partial.json
scrapper/symbol_stocks_only.json
scrapper/symbols_coins_only.json
scrapper/merged_categories_stocktwits.json
scrapper/merged_flat_stocktwits.jsonl
scrapper/batch_progress.json

# Runtime PID files
*.pid

# Local agent/tooling state (user-specific, contains absolute paths)
.claude/
.superpowers/

# Reference literature (large binaries — see docs/ for citations)
*.pdf

# Saved user profiles — whitelist seed fixtures only
analiza/profiles/*.json
!analiza/profiles/anti_esg_diversified.json
!analiza/profiles/balanced_signal.json

# Provenance-sensitive data — NOT published.
# Real yfinance-derived financials + the scraped StockTwits/CompaniesMarketCap
# universe (2500+ real company identities) and computed scores. Regenerate
# locally via the scrapers + analiza/ pipeline. The app ships with synthetic
# fixtures in demo/ so it runs without any of this.
analiza/input/company_fundamentals.csv
analiza/input/company_technicals.csv
analiza/input/company_fundamentals_template.csv
analiza/input/company_technicals_template.csv
analiza/input/fundamentals_worklist.csv
analiza/input/technicals_worklist.csv
scrapper/cmc_to_stocktwits_symbol_map.json
scrapper/cmc_to_stocktwits_symbol_stats.json
scrapper/cmc_unmatched_symbols.json
scrapper/cmc_ambiguous_symbols.json
scrapper/merge_report.json
scrapper/batch_progress_sample.json
